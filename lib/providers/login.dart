import 'package:flutter/material.dart';

class LoginProvider extends ChangeNotifier {
  bool passwordVisible = false;

  void changePasswordProvider() {
    passwordVisible != passwordVisible;

    notifyListeners();
  }
}
